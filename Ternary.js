// if person's age is more then 18,
// They are 'Adult' categery, othetrwise,
// they are a 'Child' category

let age = 17;
 
let type = age > 18 ? "Adult Ticket" : "Child Ticket"
console.log(type);

// condition ? 'Value1 : value2